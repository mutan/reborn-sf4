<form class="d-inline" method="post" action="{{ path('liquid_delete', {'id': liquid.id}) }}" onsubmit="return confirm('Точно удалить?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liquid.id) }}">
    <button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
</form>
