{% extends 'admin/layout/base_admin.html.twig' %}

{% block title %}Художники{% endblock %}

{% block header %}
    <div class="col-md-10 d-flex align-items-center justify-content-start">
        <h1><i class="fas fa-cogs"></i> Художники <small class="d-none d-lg-inline text-secondary"></small></h1>
    </div>
    <div class="col-md-2 d-flex align-items-center justify-content-end">
        <div class="dropdown create">
            <button id="liquid_new" class="btn btn-outline-light" type="button">
                <i class="fas fa-plus"></i> Добавить
            </button>
        </div>
    </div>
{% endblock %}

{% block body %}
    <table class="table table-hover datatable">
        <thead>
            <tr>
                <th width="1%"><i class="fas fa-file-image"></i></th>
                <th>Название</th>
                <th>Изображение</th>
                <th width="1%">Действия</th>
            </tr>
        </thead>
        <tbody>
        {% for liquid in liquids %}
            <tr>
                <td><img src="{{ asset(cardService.iconDir ~ liquid.image) }}" class="" alt="img"></td>
                <td>{{ liquid.name }}</td>
                <td>{{ liquid.image }}</td>
                <td>
                    {{ include('admin/layout/_edit_button.html.twig', {'route': 'liquid_edit', 'id': liquid.id}) }}
                    {{ include('admin/layout/_delete_button.html.twig', {'route': 'liquid_delete', 'id': liquid.id}) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Записей не найдено.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
